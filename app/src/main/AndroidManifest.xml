<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PoliteknikNegeriSriwijaya">

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboard Activity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- Absen Masuk Activity -->
        <activity
            android:name=".AbsenMasukActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Absen Keluar Activity -->
        <activity
            android:name=".AbsenKeluarActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Perizinan Activity -->
        <activity
            android:name=".PerizinanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Riwayat Activity -->
        <activity
            android:name=".RiwayatActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>